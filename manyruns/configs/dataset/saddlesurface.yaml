# Negative curvature — a manifold whose local geometry differs sharply from swissroll's.
# Included so "manifold" is not represented by a single curvature regime.
name: saddlesurface
handle: {kind: manylatents, ref: saddlesurface}
modality: synthetic
shape: manifold

# topology: the scShapeBench axis (additive; see vocab.TOPOLOGIES)
topology: [surface]
design:   single

# manylatents 0.1.6, seed 42: training/fit set (not the full generated tensor).
dims: {n_samples: 4000, n_features: 3}
