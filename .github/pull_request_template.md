## Problem and change

What could a user not do before, and what can they do now? Link the issue if there is one.

## Verification

- Failing test added before the behavior change (or explain why this is docs-only):
- Tests and lint run, with results:
- `co-science --version` output:
- Platform and Python version:

## Compatibility

Does this change the beta CLI or record schema? Update the changelog when it does.
Confirm that the one-way learner rule still holds and no private data or credentials
are included.
